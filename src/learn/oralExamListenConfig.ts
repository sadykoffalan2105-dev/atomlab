/** Пауза на обдумывание после вопроса (сек). */
export const ORAL_THINK_SECONDS = 20

/** Максимальная длительность одной записи ответа (сек). */
export const ORAL_LISTEN_MAX_SECONDS = 120

/** Минимум секунд записи, прежде чем можно нажать «Закончить запись». */
export const ORAL_LISTEN_MIN_SECONDS = 12
