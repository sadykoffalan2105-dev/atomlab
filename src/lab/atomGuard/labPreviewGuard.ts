/** Инварианты превью лаборатории: продукт в 3D только после синтеза. */

export function assertNoProductHeroBeforeRun(
  _runId: number,
  _productSlotVisible: boolean,
  _transformPreviewActive: boolean,
): void {
  /* silent invariant */
}
