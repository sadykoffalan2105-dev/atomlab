import { LabEntryScene } from './entry/LabEntryScene'

/**
 * Точка монтирования экрана входа в свободную лабораторию.
 *
 * Раньше здесь стоял одинокий кислород в языке CPK-сфер на масштабе 3.4 — ореол
 * радиусом r×6.2 заливал весь кадр сплошным красным свечением, и вход читался
 * как «просто круг». Теперь то же место занимает живая реакция-приглашение
 * 2 H₂ + O₂ → 2 H₂O с поясом веществ и интерактивом (src/components/lab/entry/).
 *
 * Имя и сигнатура экспорта сохранены намеренно: LabScene.tsx рендерит
 * <DecorativeAtom /> без пропсов и не меняется ни на символ. Импорт синхронный,
 * без React.lazy — Suspense внутри Canvas дал бы пустой чёрный кадр на входе.
 */
export function DecorativeAtom() {
  return <LabEntryScene />
}
