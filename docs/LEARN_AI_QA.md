# Чеклист: ИИ-учитель ATOMLAB

## Локально (`npm run dev`)

- [ ] `.env` с `OPENAI_API_KEY` — бейдж **OpenAI**, ответ на «где применяется дихромат калия» развёрнутый
- [ ] Без ключа — бейдж **Офлайн-помощник**, FAQ по кислотам/солям, честное сообщение для свободного вопроса
- [ ] Кнопки: «Пример из жизни», «Реши задачу по шагам», «Связь с уроком»
- [ ] История чата сохраняется при переключении слайдов (sessionStorage)
- [ ] Режим «Помощник» — подсказки без полного решения

## Сайт (GitHub Pages + Vercel API)

- [ ] Secret `VITE_LEARN_CHAT_URL` в GitHub → Actions
- [ ] Vercel: `OPENAI_API_KEY`, `ALLOWED_ORIGINS` с URL Pages
- [ ] Запрос с сайта не блокируется CORS

## Не-химия

- [ ] «Кто написал Войну и мир» → вежливый отказ, предложение задать химию

## Electron

- [ ] Production build с `VITE_LEARN_CHAT_URL` на Vercel endpoint
